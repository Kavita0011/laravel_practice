import React from "react";

export default function App() {
  return (
    <div style={{ padding: 20 }}>
      <h1>React is working!</h1>
    </div>
  );
}
// This code defines a simple React component that renders a heading "React is working!" inside a div with some padding. The component is exported as the default export of the module.